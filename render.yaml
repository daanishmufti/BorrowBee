services:
  - type: web
    name: borrowbee
    env: python
    buildCommand: "pip install -r render_requirements.txt"
    startCommand: "gunicorn --bind 0.0.0.0:$PORT main:app"
    envVars:
      - key: SESSION_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: borrowbee-db
          property: connectionString

databases:
  - name: borrowbee-db
    databaseName: borrowbee
    user: borrowbee